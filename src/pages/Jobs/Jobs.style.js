import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  innerContainer: {
    flex: 1,
    paddingTop: 10,
    paddingBottom: 10,
    padding: 5,
  },
});

export default styles;
